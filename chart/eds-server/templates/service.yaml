apiVersion: v1
kind: Service
metadata:
  name: {{ include "eds-server.fullname" . }}
  labels:
    {{- include "eds-server.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "eds-server.selectorLabels" . | nindent 4 }}
